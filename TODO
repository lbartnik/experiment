* compare all objects of a given class, returned by `stashed()`
* find a commit parenting a given object
* find a full sequence of calls leading to a given object
* UI:
  - show history of commits
  - print commits
  - do not print commits in stashed()

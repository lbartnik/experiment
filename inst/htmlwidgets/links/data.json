{
  "steps": [
    {
      "id": "1",
      "type": "object",
      "name": "input",
      "expr": ["input <- readr::read_csv(\"halfhourly/block_62.csv\", na = \"Null\") %>% ", "    rename(meter = LCLid, timestamp = tstp, usage = `energy(kWh/hh)`) %>% ", "    filter(meter %in% c(\"MAC004929\", \"MAC000010\", \"MAC004391\"), ", "        year(timestamp) == 2013)"],
      "desc": "data.frame [3x8192]"
    },
    {
      "id": "2",
      "type": "object",
      "name": "input",
      "expr": ["input %<>% mutate(timestamp = floor_date(timestamp, \"hours\")) %>% ", "    group_by(meter, timestamp) %>% summarise(usage = sum(usage))"]
    },
    {
      "id": "3",
      "type": "plot",
      "contents": "3_min.png",
      "expr": ["with(filter(input, meter == \"MAC004929\"), plot(timestamp, usage, ", "    type = \"p\", pch = \".\"))"]
    }
  ],
  "links": [
    {
      "target": 2,
      "source": 1
    },
    {
      "target": 3,
      "source": 2
    }
  ]
}
